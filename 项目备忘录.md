# Find Partner 校园搭子 · 项目备忘录

> 最后更新：2026-09-19
> 这个文件记录了项目所有关键信息，忘的时候翻这里。

---

## 一、项目基本信息

| 项目 | 内容 |
|---|---|
| 项目名 | Find Partner（校园搭子） |
| 项目位置 | `D:\找搭子` |
| 技术栈 | Python Flask + SQLite + H5网页 |
| 主程序 | `D:\找搭子\app.py` |
| 网页模板 | `D:\找搭子\templates\` 文件夹 |
| 样式文件 | `D:\找搭子\static\css\style.css` |
| 数据库 | `D:\找搭子\database.db`（Git不存这个） |
| 开发工具 | PyCharm |

---

## 二、日常启动（每次开机后必做）

> ⚠️ **现在还没买云服务器，你电脑就是服务器。关机=网站停。**

### 开机恢复3步（每天开机按这个来）

**第1步：启动Flask（网站后端）**
打开PyCharm → 打开 `D:\找搭子\app.py` → 点绿色三角▶
- 看到 `🚀 校园搭子APP启动成功` 就是好了
- 本地地址：http://127.0.0.1:5000

**第2步：启动cpolar（公网隧道）**
按 `Win+R` → 输入 `powershell` → 回车，粘贴：
```
cpolar http 5000
```
- 等5秒，会显示 `Forwarding https://xxxx.r10.cpolar.top`
- **复制那个https网址**，这就是新公网地址
- ⚠️ 这个PowerShell窗口不能关，关了公网就断

**第3步：发给同学**
- cpolar免费版每次重启地址都会变
- 把新网址发给同学
- 如果桌面exe里写的是旧地址，要改 `desktop-app\main.js` 里的APP_URL重新打包

> ⚠️ **三个都不能关**：PyCharm的Flask、cpolar的PowerShell窗口、你电脑本身。
> 想让你关机网站也活着 → 买云服务器部署（见第十章）。

---

## 三、公网地址

### 当前地址（会变）
```
https://404009a3.r10.cpolar.top
```
> 这是cpolar免费版随机给的，**重启cpolar后会变**。变了就重新复制新地址发给同学。

### cpolar后台
- 本地管理页：http://localhost:4042
- 官网后台：https://dashboard.cpolar.com
- 账号：pdx

---

## 四、Git版本管理（防止代码丢）

### 提交代码（改完功能就提交一次）
在PyCharm底部点 **Terminal**（终端），输入：
```
cd D:\找搭子
git add .
git commit -m "写一句这次改了啥"
```

### 常用场景
| 想做什么 | 命令 |
|---|---|
| 保存当前进度 | `git add .` 然后 `git commit -m "说明"` |
| 看看改了啥 | `git status` |
| 历史版本 | `git log --oneline` |

### 重要规则
- **database.db 不要提交**（已经在.gitignore里自动忽略了，放心）
- **改代码前最好先commit**，搞坏了能回退
- 用PyCharm图形界面也行：左侧Git → 点文件 → Commit

---

## 五、加新功能的标准流程

每次想加新功能，按这个顺序：

### 1. 想清楚要加什么
- 是新页面？还是新按钮？还是新数据（比如新字段）？

### 2. 改数据库（需要存新东西时）
打开 `app.py` 里的 `init_db()` 函数，加新表或新字段。
> SQLite加字段用 `ALTER TABLE`，不能加"时间戳默认值"这种动态默认。

### 3. 写后端路由
在 `app.py` 里加 `@app.route('/xxx')` 函数：
- 网页路由：返回 `render_template('xxx.html')`
- 接口路由：返回 `jsonify({...})`

### 4. 写前端页面
在 `templates\` 文件夹新建或改 `.html` 文件。
- 改样式去 `static\css\style.css`

### 5. 测试
1. 停掉Flask再重启（debug关了，不重启不生效）
2. 浏览器 `Ctrl+Shift+R` 强刷
3. 自己测一遍功能

### 6. 提交
```
git add .
git commit -m "新增：xxx功能"
```

---

## 六、数据库备份（重要！）

数据库文件就是 `D:\找搭子\database.db`。

### 备份方法（每周一次）
1. 右键 `database.db` → 复制
2. 粘贴到 `D:\找搭子\备份\` 文件夹
3. 改名为 `database_20260919.db`（带日期）

### 恢复
把备份文件改回 `database.db`，覆盖回去就行。

> ⚠️ 数据库是你所有用户、帖子、聊天记录的家。代码丢了能重写，数据库丢了用户数据就没了。**定期备份！**

---

## 七、已经做了哪些功能（清单）

- [x] 用户注册/登录（手机号+密码）
- [x] 用户等级+经验系统（发帖+10，评论+2，被点赞+2，被关注+3）
- [x] 发帖子（文字+图片）
- [x] 评论（楼中楼，可以回复别人的评论）
- [x] 点赞 / 收藏
- [x] 拉黑名单
- [x] 互相关注/好友（贴吧式：关注+粉丝）
- [x] 搜索用户名找人
- [x] 学生实名认证（认证后显示🎓标）
- [x] 在线状态显示（可开关）
- [x] 私聊聊天（文字+位置分享）
- [x] 公共聊天大厅（全网广场 + 同校圈）
- [x] 找搭子（发位置+显示距离）
- [x] 学校显示隐私开关
- [x] 返回上一页按钮
- [x] UI高级化（紫色渐变、圆角卡片）

---

## 八、常见问题

### Q: 改了代码没生效？
A: debug关了，必须**停掉Flask重新运行**。然后浏览器按 `Ctrl+Shift+R` 强刷。

### Q: 同学打不开网站？
A: 检查三个：
1. 你PyCharm里Flask在跑吗？
2. cpolar在跑吗？（PowerShell窗口关了没）
3. 你电脑联网吗？

### Q: 网站卡住、按钮按不动？
A: 已经改了多线程（threaded=True），重启Flask就好。如果还卡，看是不是Python报错。

### Q: 公网地址变了？
A: 正常，cpolar免费版每次重启都变。新地址在cpolar启动日志里找。

### Q: 怎么让网址固定不变？
A: 两个办法：
1. cpolar付费买固定域名（约10元/月）
2. 买云服务器正式部署（学生机9元/月，自己的域名）

---

## 九、技术概念（不懂的看这里）

| 名词 | 大白话 |
|---|---|
| Flask | Python写网站的工具，负责"后端"（逻辑、数据库） |
| SQLite | 一个文件数据库，所有用户数据都存在 `database.db` 这一个文件里 |
| Jinja2 | 让网页能显示Python数据的模板引擎 |
| HTML | 网页长什么样 |
| CSS | 网页的样式（颜色、圆角、布局） |
| Git | 代码备份工具，能回到以前的版本 |
| cpolar | 把你电脑的网站"转发"到公网，让别人能访问 |
| 路由(route) | 网址和代码函数的对应关系，比如 `/login` 对应登录函数 |
| 模板(template) | 网页文件，放在templates文件夹 |

---

## 十、Electron桌面客户端打包（Windows exe）

> 目标：把网页套成独立桌面软件，像微信一样有窗口，不用开浏览器。

### 项目位置
- Electron项目文件夹：`D:\找搭子\desktop-app\`
- 主文件：`main.js`（写着要加载的公网地址）
- 配置文件：`package.json`
- 打包产物：`D:\找搭子\desktop-app\dist\` 里的 `FindPartner.exe`

### 当前进度（2026-09-19）
- ✅ Node.js v22 已装
- ✅ Electron v31.7.7 + electron-builder 已 npm install 完成（装了13分钟）
- ⏸️ 打包进行到一半被你停了（dist文件夹还没出exe）
- 下次继续：打开 PowerShell → `cd D:\找搭子\desktop-app` → 跑打包命令（见下）

### 关键文件 main.js
里面这一行**必须和当前cpolar公网地址一致**：
```js
const APP_URL = 'https://404009a3.r10.cpolar.top';
```
> ⚠️ **cpolar每次重启地址会变！** 地址变了就要改这一行，重新打包exe，不然同学打开exe是旧网址连不上。

### 打包命令（每次改了地址都要跑一遍）
打开 PowerShell：
```bash
cd D:\找搭子\desktop-app
$env:ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"
$env:ELECTRON_BUILDER_BINARIES_MIRROR="https://npmmirror.com/mirrors/electron-builder-binaries/"
npx electron-builder --win
```
跑完去 `dist\FindPartner.exe` 拿成品，双击就能打开。

### 本地预览（不打包，先看效果）
```bash
cd D:\找搭子\desktop-app
npx electron .
```
会弹出软件窗口。

### ⚠️ 重要原理（别搞混）
- **exe只是外壳浏览器**，不包含你的Flask后端和数据库
- 打开exe时，**你电脑必须同时跑着 Flask + cpolar**
- 你电脑关机 → 同学打开exe就白屏/连不上
- 想让你关机别人也能用 → 必须买云服务器部署

### 打包出来的exe发给同学
直接把 `FindPartner.exe` 发给同学，双击就能用，不用装任何东西。
前提：你电脑Flask+cpolar在跑，且exe里的地址和当前cpolar地址一致。

---

## 十一、下一步可以做什么

- [ ] 买云服务器正式部署，固定网址
- [ ] 消息推送（别人给你发消息时手机响）
- [ ] 搭子匹配推荐（根据兴趣自动推荐）
- [ ] 帖子点赞数显示
- [ ] 个人主页装修（背景图）
- [ ] 举报功能
- [ ] 发布HBuilderX打包成手机APP
- [ ] Electron打包完成（当前进度：依赖装好，exe未出）

---

> 📌 忘了什么就翻这个文件。改了项目记得更新这里。
> 有问题问阿包！
