# 🎒 校园搭子APP · 项目开发备忘录

> 本文件记录项目从0到1的全过程，防止忘记。遇到问题先看这里。
> 最后更新：2026-09-17

---

## 一、项目是什么

**校园搭子APP**：一个让校园用户发布"找搭子"帖子（运动/学习/游戏/生活）、互相联系的小应用。

- **第1迭代目标**：简易可用的APP框架（登录注册、发帖、看帖、个人中心）
- **本质**：H5网页（Flask后端渲染）+ 安卓外壳（HBuilderX打包）＝看起来像App的软件

---

## 二、文件都在哪（重要！）

| 内容 | 位置 |
|---|---|
| **后端代码项目**（主项目） | `D:\找搭子\` |
| 主程序 | `D:\找搭子\app.py` |
| 数据库（用户/帖子数据） | `D:\找搭子\database.db` |
| 页面模板 | `D:\找搭子\templates\`（5个html） |
| 样式 | `D:\找搭子\static\css\style.css` |
| **安卓打包项目** | `D:\dazi-app\dazi-app\` |
| 打包配置文件 | `D:\dazi-app\dazi-app\manifest.json` |
| **APK安装包输出** | `D:\dazi-app\dazi-app\unpackage\release\apk\` |
| HBuilderX软件 | `D:\HBuilderX\app\HBuilderX\HBuilderX.exe` |

> 📌 原项目文件夹名"豆包的软件开发"已改名为"找搭子"（2026-09-17）

---

## 三、技术栈

- **后端**：Python + Flask（轻量Web框架）
- **数据库**：SQLite（文件型数据库，无需安装，就是 database.db）
- **前端**：HTML5 + 原生CSS/JS（手机浏览器适配）
- **安卓打包**：HBuilderX（5+App，把网页包成安卓APK）
- **登录态**：Flask Session

---

## 四、怎么启动项目（最重要！）

### 方式一：命令行启动
```
cd D:\找搭子
python app.py
```
看到 `校园搭子APP启动成功` 就OK，浏览器打开 `http://127.0.0.1:5000`

### 方式二：PyCharm启动
1. 打开PyCharm → **File → Open** → 选择 `D:\找搭子`
2. 右键 `app.py` → **Run 'app'**
3. 浏览器打开 `http://127.0.0.1:5000`

### 手机访问（测试用）
- 手机连**和电脑同一个WiFi**
- 手机浏览器打开 `http://电脑IP:5000`（电脑IP用 `ipconfig` 查，当前是 `192.168.194.131`）

---

## 五、怎么打包新版本APK（HBuilderX流程）

代码改完后，重新打包的完整流程：

1. 打开HBuilderX：`D:\HBuilderX\app\HBuilderX\HBuilderX.exe`
2. **文件 → 打开目录** → 选择 `D:\dazi-app\dazi-app`
3. 改配置：双击 `manifest.json` → 如果电脑IP变了，改 `launch_path` 里的地址
4. **发行 → 原生App-云打包**
5. 平台勾 **Android**，证书选 **云端证书**（已配置过）
6. 点 **打包** → 等待3-10分钟
7. APK自动下载到 `D:\dazi-app\dazi-app\unpackage\release\apk\`

### ⚠️ 电脑IP变了怎么办？
WiFi重启/换网络后，电脑IP可能变化。这时：
1. 命令行输入 `ipconfig` 查新的IPv4地址（如 192.168.x.x）
2. 修改 `D:\dazi-app\dazi-app\manifest.json` 里的 `launch_path`
3. 按上面流程重新打包

---

## 六、手机上怎么用

1. 把APK发到手机（**用数据线或QQ，别用微信**——微信会给apk加`.1`后缀导致装不了）
2. 手机点击APK安装（提示"未知来源"就点"继续安装"）
3. 手机连**和电脑同一个WiFi**
4. 电脑上Flask服务保持运行（别关黑窗口）
5. 打开"校园搭子"App

---

## 七、已完成的功能（对应PRD）

- ✅ 注册/登录（手机号+密码，密码加密存储）
- ✅ 首页帖子列表（按时间倒序）
- ✅ 分类筛选（运动/学习/游戏/生活/其他）
- ✅ 发布帖子（标题/分类/内容/联系方式，带校验）
- ✅ 帖子详情页（联系方式一键复制）
- ✅ 个人中心（我的帖子、删除帖子-软删除）
- ✅ 安卓APK打包（第1版，8.2MB）

---

## 八、踩过的坑（血泪经验）

1. **微信传APK装不了**：微信会把 `xxx.apk` 改成 `xxx.apk.1`，系统不认。→ 用数据线/QQ传，或手机文件管理器改名去掉`.1`
2. **App图标报错"不是合法png"**：生成的图是JPG格式改了后缀名。→ 必须转成真正的PNG格式
3. **云打包报"通讯录权限"**：manifest默认带通讯录权限，新规要求实名认证。→ 把 `permissions` 里的 `Contacts` 和 Android权限里的 `READ_CONTACTS`/`WRITE_CONTACTS` 删掉
4. **公共测试证书不能用了**：DCloud新规，新应用必须用云端证书。→ 打包时证书选"云端证书"（免费，在DCloud控制台一键生成）
5. **手机白屏**：最大原因手机没连同一个WiFi（用5G流量访问不到电脑局域网）。→ 手机切到和电脑同一个WiFi
6. **Flask启动方式**：`python app.py` 要在 `D:\找搭子` 目录下运行

---

## 九、下一步建议（第2迭代）

- [ ] 部署到公网服务器（阿里云/腾讯云学生机），所有人随时能访问（不用同一WiFi）
- [ ] 部署后把 manifest.json 的 launch_path 改成公网地址，重新打包
- [ ] 页面细节优化（空状态、加载提示、错误提示）
- [ ] 功能扩展（按PRD第2迭代：帖子详情优化、用户信息完善）
- [ ] 用Git做版本管理（防止代码丢失）

---

## 十、常用命令速查

```bash
# 启动后端
cd D:\找搭子
python app.py

# 查看电脑IP（手机访问要用）
ipconfig

# 测试服务是否正常（浏览器也能测）
curl http://127.0.0.1:5000/

# 打包配置位置
D:\dazi-app\dazi-app\manifest.json
```

---

**记住一句话**：改代码 → 跑Flask → HBuilderX重新打包 → 传到手机测试。循环往复，项目就长大了！
